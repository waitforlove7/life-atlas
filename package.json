{
  "name": "atlas",
  "version": "0.1.0",
  "private": true,
  "description": "Your Personal World Atlas",
  "scripts": {
    "dev:web": "npm run dev --prefix apps/web",
    "dev:api": "cd apps/api && uvicorn main:app --reload --port 8000",
    "build": "npm run build --prefix apps/web"
  }
}
